<div class="container mt-4">
  <h2>Bonjour sur votre espace admin <%= current_user.full_name %> 👋</h2>

  <p>
    Utilisez la barre de navigation pour parcourir les différentes pages.
  </p>

  <div class="mt-4">
    Vous possèdez les rôles suivants :
    <% if current_user.roles.any? %>
      <ul>
        <% current_user.roles.pluck(:name).each do |role| %>
          <li><span class="badge badge-info text-larger"><%= role %></span></li>
        <% end %>
      </ul>
    <% else %>
      <em>Aucun role</em>
    <% end %>
  </div>
</div>
