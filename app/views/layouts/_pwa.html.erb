<button
  class="fixed-bottom btn btn-primary"
  data-controller="service-worker pwa"
  data-service-worker-path-value="<%= service_worker_path(format: :js)%>"
  data-action="beforeinstallprompt@window->pwa#configure"
>
  <i class="fas fa-download"></i> Installer l'application
</button>
